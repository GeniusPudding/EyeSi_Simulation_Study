# CLAUDE.md — 本專案工作規範

## ⭐ 文件必須嚴格根據論文原文(最重要的一條)

`docs/` 底下所有整理的內容,**每個技術性敘述都必須可追溯到 `papers/` 的一手論文原文**。

- **可追溯**:凡陳述某方法「怎麼做 / 用什麼 / 效能多少」,須對應到論文的**章節 / 公式 / 圖**——能標就標(如「§4.3」「Eq.4」「Fig.2」)。
- **推論要明標**:論文沒寫、但為教學補的內容(未給的公式、SOFA 類別名、初始化細節等),一律標 **[推論]** 或 **[補充]**,**絕不當成論文原文陳述**。`reference/8_inria_implementation_deepread.md` 已示範此慣例。
- **原文優先於既有筆記**:當既有筆記與原文衝突,**以原文為準**,並就地修正筆記。已這樣校正過的例子:撕裂判定是 **stress**(非 strain)特徵值;「~5 FPS」是 **IOL 那步**(水晶體 phaco 其實 80 fps),非全系統;Dequidt 系統**無力回饋觸覺裝置**(是 IR 光學追蹤);非同步預條件子屬 **Courtecuisse 2010**(Dequidt 只引用)。
- **以主要論文為準**:
  - INRIA 路線 → **Comas 2008 / Allard(Marchal)2009 / Courtecuisse 2010 / Dequidt 2013** 原文。
  - EyeSi 路線 → **Weber 2006 / Weber 2009** 原文。
  - 離線力學 → **Han 2021** 原文。
- **不確定就寫不確定**:寧可寫「論文未明述」也不要編造;付費牆取不到全文時,標明依據層級(摘要 / 書目 / 檢索補充)。

## 語言與格式

- `docs/` 沿用**繁體中文**(既有慣例);程式碼註解 / 識別字用英文。
- 數學一律**純 Unicode**(如 `ρü = ∇·σ + f`),**不用 LaTeX**(終端無法渲染)。

## 文件結構(去重原則)

- `overview.md` 頂端 = **START HERE 主幹**(閱讀順序)+ **概念地圖**(知識串接)。
- **機制正本只寫一次**:引擎/數值 → `engines/`;撕裂準則 → `ccc_method/4`;INRIA 整合 → `reference/8`。
- `reference/` 是**查閱層**(論文血緣 + 貢獻定位 + 指向正本),**不重述機制**。
- 新增內容前先查有無既有正本,避免重複(6/7、5/6 曾發生重疊)。

## 提交

- 作者僅用使用者本人帳號;**不加** AI 的 `Co-Authored-By` 或「Generated with」footer。
