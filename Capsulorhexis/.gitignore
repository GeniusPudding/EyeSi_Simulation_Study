# build output
build/
# third-party tools fetched by scripts/setup.ps1 (too large / reproducible)
tools/
# logs
*.log

# per-session diagnostics (cap_membrane LOG_DIAG + run_cap.sh tee)
scenes/diag_last.csv
scenes/run_last.log
# per-session sigma1 field recording (cap_membrane CAP_STRESS_LOG)
scenes/stress_log.jsonl
scenes/stress_log.jsonl.prev
